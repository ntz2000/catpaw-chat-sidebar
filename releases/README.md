# Workspace VSIX Archives

This folder contains every VSIX package produced during development.

Install the newest version unless you need to roll back. In CatPaw or VS Code,
run **Extensions: Install from VSIX...**, select the package, then run
**Developer: Reload Window**.

Current newest build: `catpaw-chat-sidebar-0.0.18.vsix`.
